apiVersion: apps/v1
kind: Deployment
metadata:
  name: loan-product-service
  namespace: loan-approval
  labels:
    app: loan-product-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: loan-product-service
  template:
    metadata:
      labels:
        app: loan-product-service
    spec:
      containers:
      - name: loan-product-service
        image: loan-approval/loan-product-service:latest
        ports:
        - containerPort: 8082
        env:
        - name: SPRING_PROFILES_ACTIVE
          valueFrom:
            configMapKeyRef:
              name: loan-config
              key: SPRING_PROFILES_ACTIVE
        - name: DB_URL
          value: "jdbc:postgresql://loanproductdb-service.loan-approval.svc.cluster.local:5432/loanproductdb"
        - name: DB_USERNAME
          valueFrom:
            secretKeyRef:
              name: loan-secrets
              key: db-username
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: loan-secrets
              key: db-password
        - name: EUREKA_SERVER_URL
          valueFrom:
            configMapKeyRef:
              name: loan-config
              key: EUREKA_SERVER_URL
        - name: KAFKA_SERVERS
          valueFrom:
            configMapKeyRef:
              name: loan-config
              key: KAFKA_SERVERS
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8082
          initialDelaySeconds: 60
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8082
          initialDelaySeconds: 30
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: loan-product-service
  namespace: loan-approval
  labels:
    app: loan-product-service
spec:
  type: ClusterIP
  ports:
  - port: 8082
    targetPort: 8082
  selector:
    app: loan-product-service
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: loan-product-service-hpa
  namespace: loan-approval
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: loan-product-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
